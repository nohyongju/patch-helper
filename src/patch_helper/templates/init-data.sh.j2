#!/bin/bash
# ===========================================
# 패치가이드: {{ repo }} {{ from_ref }} → {{ to_ref }}
# 생성일: {{ generated_date }}
# 초기 데이터 실행 스크립트
# ===========================================

{{ curl_script }}
