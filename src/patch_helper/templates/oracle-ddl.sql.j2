-- ===========================================
-- 패치가이드: {{ repo }} {{ from_ref }} → {{ to_ref }}
-- 생성일: {{ generated_date }}
-- 실행 시점: 배포 전
-- ===========================================

{{ oracle_ddl }}
